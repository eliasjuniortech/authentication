services:
  api:
    build: ./
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    volumes:
      - redis_data:/data

volumes:
  redis_data:
